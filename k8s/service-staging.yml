apiVersion: v1
kind: Service
metadata:
  name: featureflags-api-service
  namespace: featureflags-staging
  labels:
    app: featureflags-hub
    environment: staging
spec:
  ports:
  - port: 8001
    targetPort: 8000
    nodePort: 30001
    protocol: TCP
    name: http
  selector:
    app: featureflags-hub
    environment: staging